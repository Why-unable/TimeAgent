你正在参与开发一个名为 **Time Agent** 的全栈 Agent 项目。

仓库根目录中已经存在两份架构规范：

* `PROJECT_SPEC.md`：后端、领域模型、LangChain/LangGraph Agent、Memory、Middleware、Celery、简报工作流等总体规范；
* `FRONTEND_SPEC.md`：React 前端、Agent 流式交互、审批界面、日历、任务、提醒、简报及前后端部署规范。

你的当前任务不是实现整个项目，而是完成项目正式开发前的准备工作，并搭建一个可运行、可测试、可持续迭代的基础工程。

---

# 一、首先阅读和分析规范

完整阅读：

```text
PROJECT_SPEC.md
FRONTEND_SPEC.md
```

在开始修改代码前，先检查：

1. 当前仓库已经存在哪些目录和文件；
2. 是否已经存在 Django、React、Docker Compose 或其他工程代码；
3. 当前实现与两份规范是否存在冲突；
4. 两份规范之间是否存在字段、接口、目录或职责不一致；
5. 哪些设计属于当前 MVP；
6. 哪些设计属于后续扩展，当前不应实现。

如果仓库已有代码，应优先兼容和复用现有结构，不得未经说明直接删除或重写现有实现。

---

# 二、本次任务目标

本次只完成以下准备工作：

## 1. 整理项目开发规则

在仓库根目录创建或完善：

```text
AGENTS.md
ROADMAP.md
README.md
.env.example
Makefile
```

### `AGENTS.md`

根据两份规范提炼 Coding Agent 必须遵守的强制规则，至少包括：

1. PostgreSQL 是业务事实来源；
2. Agent Tool 不得直接调用 Django ORM；
3. 所有写操作必须经过 Application Service；
4. 数据库时间统一保存为 UTC；
5. 用户时间必须结合 IANA 时区解析；
6. Time Steward Agent 使用 LangChain `create_agent()`；
7. 外层 LangGraph 只负责触发路由、Handoff 和工作流编排；
8. 定时提醒不经过 LLM；
9. 定时简报直接进入 Briefing Workflow；
10. 高风险操作必须通过 ActionProposal 和 HITL；
11. Agent 必须配置模型调用和工具调用限制；
12. Memory 不能替代业务数据库；
13. 外部服务必须通过 Provider 接口接入；
14. 前端不得复制后端业务规则；
15. 新增或修改 API 后必须更新 OpenAPI Schema 和前端类型；
16. 所有新功能必须包含测试；
17. 不得记录 API Key、Token 或敏感信息；
18. 未明确要求时不得修改任务范围之外的文件；
19. 当前阶段不得提前实现大规模多 Agent 系统；
20. 不得为了“架构完整”创建没有实际用途的空抽象层。

### `ROADMAP.md`

把项目拆成可独立验收的小阶段：

```text
Phase 0：工程骨架
Phase 1：UserPreference 与时间基础
Phase 2：提醒完整闭环
Phase 3：CalendarEvent、Task 与 Today 页面
Phase 4：LangGraph 基础设施
Phase 5：Time Steward Agent
Phase 6：ActionProposal 与 HITL
Phase 7：Briefing Workflow
Phase 8：天气与新闻
Phase 9：外部日历和通知渠道
Phase 10：生产部署与监控
```

每个阶段需要写清：

* 目标；
* 后端任务；
* 前端任务；
* 测试要求；
* 验收标准；
* 明确不包含的内容。

同时创建前 20 个左右的开发任务编号，例如：

```text
T001 初始化 Monorepo
T002 初始化 Django
T003 初始化 React
T004 配置 PostgreSQL 和 Redis
T005 配置 Celery
T006 添加健康检查
...
```

任务必须足够小，原则上一个任务对应一个独立提交或 Pull Request。

---

# 三、建立 Monorepo 骨架

如果仓库尚未初始化，请按照以下结构建立基础目录：

```text
time-agent/
├── README.md
├── PROJECT_SPEC.md
├── FRONTEND_SPEC.md
├── AGENTS.md
├── ROADMAP.md
├── docker-compose.yml
├── .env.example
├── Makefile
│
├── backend/
│   ├── manage.py
│   ├── pyproject.toml
│   ├── uv.lock
│   ├── config/
│   ├── apps/
│   └── tests/
│
├── frontend/
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── src/
│   └── tests/
│
├── infra/
│   ├── nginx/
│   └── monitoring/
│
└── docs/
    ├── architecture/
    ├── api/
    └── decisions/
```

如果当前仓库已经存在合理结构，不要强行移动全部文件。应说明现有结构与规范结构的映射关系，并进行最少必要调整。

---

# 四、后端基础工程

建立一个最小可运行的 Django 工程。

本阶段允许安装和配置：

* Python 3.12；
* Django；
* Django REST Framework；
* PostgreSQL 驱动；
* Redis；
* Celery；
* Celery Beat；
* OpenAPI Schema 工具；
* pytest；
* pytest-django；
* Ruff；
* mypy 或 pyright。

建立：

```text
backend/config/
backend/apps/
backend/tests/
```

配置：

* development；
* test；
* production；

三套 settings。

至少实现：

```text
GET /health/live
GET /health/ready
GET /api/schema/
```

要求：

### `/health/live`

只判断 Django 进程是否存活，不依赖数据库。

### `/health/ready`

检查：

* PostgreSQL；
* Redis；

是否可用，并返回结构化结果。

示例：

```json
{
  "status": "ready",
  "checks": {
    "database": "ok",
    "redis": "ok"
  }
}
```

当前阶段不要实现：

* CalendarEvent；
* Task；
* Reminder；
* Agent；
* Briefing；
* Google Calendar；
* 天气和新闻；
* 完整认证业务。

可以使用 Django 默认用户模型，为后续扩展保留空间。

---

# 五、前端基础工程

建立最小可运行的 React 工程。

推荐使用：

* React；
* TypeScript；
* Vite；
* React Router；
* TanStack Query；
* Zustand；
* React Hook Form；
* Zod；
* Tailwind CSS；
* Vitest；
* React Testing Library；
* Playwright；
* MSW。

当前阶段至少建立：

```text
frontend/src/app/
frontend/src/pages/
frontend/src/features/
frontend/src/components/
frontend/src/api/
frontend/src/stores/
frontend/src/utils/
```

至少实现：

* 基础 App Layout；
* 桌面侧边导航；
* 移动端基础导航占位；
* 首页或系统状态页面；
* 后端健康检查请求；
* 全局错误边界；
* TanStack Query Provider；
* API Client；
* 环境变量配置。

系统状态页应显示：

```text
Frontend：正常
Django：正常或异常
PostgreSQL：正常或异常
Redis：正常或异常
```

当前阶段不要实现：

* 完整聊天页面；
* Calendar 页面；
* Tasks 页面；
* Reminders 页面；
* Approval Card；
* Briefing 配置界面；
* Agent SSE。

可以创建路由占位页面，但不要填充虚假的完整业务功能。

---

# 六、OpenAPI 与前后端契约

从项目开始阶段就建立 OpenAPI 类型同步机制。

要求提供命令：

```bash
make api-schema
make frontend-api
```

其中：

### `make api-schema`

从 Django 生成 OpenAPI 文件，例如：

```text
backend/openapi.json
```

### `make frontend-api`

根据 OpenAPI 文件生成 TypeScript API 类型或客户端到：

```text
frontend/src/api/generated/
```

如果当前只能先建立脚本框架，也必须：

* 给出具体工具选择；
* 写入执行命令；
* 在 README 中说明；
* 确保后续可以实际生成，而不是保留无效占位脚本。

前端页面不得直接散落原始 `fetch()` 调用。

统一通过：

```text
frontend/src/api/client.ts
```

访问后端。

---

# 七、Docker Compose

建立可运行的：

```text
docker-compose.yml
```

至少包含：

```text
frontend
django
celery-worker
celery-beat
postgres
redis
nginx
```

开发阶段可以根据实际情况使用 bind mount 和热重载。

要求：

1. PostgreSQL 和 Redis 配置 Healthcheck；
2. Django 在数据库可用后启动；
3. Celery Worker 和 Beat 使用相同 Django 配置；
4. Nginx 提供前端并代理 Django API；
5. 环境变量集中从 `.env` 读取；
6. 不允许在 Compose 文件中硬编码真实密码；
7. 日志应具有合理的大小限制；
8. 数据库数据使用命名 Volume 保存。

最终应可以通过：

```bash
docker compose up --build
```

启动整套基础环境。

---

# 八、Nginx 基础配置

Nginx 当前阶段至少支持：

```text
/                  React SPA
/api/              Django API
/admin/            Django Admin
/static/           Django 静态资源
/health/           Django 健康检查
```

React Router 需要：

```nginx
try_files $uri $uri/ /index.html;
```

但以下路径不能回退到前端：

```text
/api/
/admin/
/static/
/health/
```

虽然当前阶段尚未实现 SSE，但可以在配置中预留清晰的 SSE Location，并注明：

* 关闭代理缓冲；
* 延长读取超时；
* 禁用缓存。

不要创建与实际后端路径不一致的伪配置。

---

# 九、Makefile 和开发命令

至少提供：

```bash
make up
make down
make build
make logs
make backend-test
make frontend-test
make lint
make check
make migrate
make migrations
make api-schema
make frontend-api
```

README 中说明每个命令的作用。

如果开发环境使用 `uv` 和 `npm`，也应给出不依赖 Docker 的本地运行方式。

---

# 十、测试和质量检查

本次至少建立：

## 后端

* 健康检查测试；
* Django system check；
* pytest 配置；
* Ruff 配置；
* 类型检查配置。

## 前端

* 系统状态页面组件测试；
* API Client 基础测试；
* Vitest 配置；
* React Testing Library 配置；
* Playwright 基础配置。

## 集成验证

至少验证：

1. Django 能连接 PostgreSQL；
2. Django 能连接 Redis；
3. Celery Worker 能正常加载 Django App；
4. Celery Beat 可以启动；
5. 前端能够请求 `/health/ready`；
6. Nginx 可以正确代理前后端；
7. OpenAPI Schema 可以生成。

---

# 十一、README 内容

README 至少包含：

1. 项目简介；
2. 当前开发阶段；
3. 技术栈；
4. 目录结构；
5. 环境要求；
6. 环境变量配置；
7. Docker 启动方法；
8. 本地启动方法；
9. 测试和静态检查命令；
10. OpenAPI 类型生成流程；
11. 当前已经实现的内容；
12. 当前尚未实现的内容；
13. 下一步推荐任务。

必须明确说明：

```text
当前仅完成 Phase 0 工程骨架，
尚未实现提醒、日程、任务、Agent 和简报业务。
```

---

# 十二、本次禁止事项

本次任务禁止：

1. 一次性实现整个项目；
2. 实现 CalendarEvent、Task、Reminder 等正式业务模型；
3. 实现 Time Steward Agent；
4. 创建没有业务支持的伪 Agent；
5. 实现完整聊天界面；
6. 实现天气、新闻、Gmail 或 Google Calendar；
7. 引入 Kubernetes；
8. 引入微服务；
9. 引入向量数据库；
10. 引入复杂 RBAC；
11. 创建大量没有实际内容的空文件；
12. 在未运行测试的情况下声称工程可用；
13. 修改或删除 `PROJECT_SPEC.md` 和 `FRONTEND_SPEC.md`；
14. 因为规范内容较多而擅自改变核心技术选择；
15. 使用假接口掩盖后端未完成状态。

---

# 十三、决策记录

如果实现过程中发现两份规范存在冲突，或者必须做架构选择，请创建 ADR：

```text
docs/decisions/0001-xxx.md
```

ADR 至少包含：

```text
标题
状态
背景
决策
备选方案
原因
影响
```

不要静默做出重要架构变更。

本阶段至少可以根据实际情况记录：

```text
0001-monorepo-structure.md
0002-openapi-code-generation.md
0003-development-container-topology.md
```

只在存在真实决策时创建，不要为了凑数量制造 ADR。

---

# 十四、执行方式

请按照以下顺序工作：

1. 阅读两份规范；
2. 检查当前仓库；
3. 输出简短的差异和风险说明；
4. 规划本次修改；
5. 创建或调整工程骨架；
6. 配置后端；
7. 配置前端；
8. 配置 Docker Compose 和 Nginx；
9. 配置 OpenAPI 类型生成；
10. 添加测试；
11. 运行所有可运行的检查；
12. 修复本次修改引入的问题；
13. 更新 README、AGENTS.md 和 ROADMAP.md；
14. 输出最终结果。

不要在开始实现后反复询问普通细节。遇到小型不确定项时，根据规范和最小实现原则做出合理选择，并在最终结果中说明。

---

# 十五、完成后的输出格式

完成后请按照以下格式汇报：

## 1. 规范理解

简要说明你从：

* `PROJECT_SPEC.md`
* `FRONTEND_SPEC.md`

提取出的当前阶段目标和关键约束。

## 2. 仓库现状

说明：

* 原有结构；
* 已复用内容；
* 发现的冲突；
* 做出的兼容处理。

## 3. 已完成内容

按照：

```text
工程结构
后端
前端
Docker
Nginx
OpenAPI
测试
文档
```

分类列出。

## 4. 修改文件

列出所有新增和修改文件。

## 5. 验证结果

列出实际执行过的命令及其结果，例如：

```text
docker compose config
python manage.py check
pytest
ruff check
npm test
npm run build
```

不得把未执行的检查写成已通过。

## 6. 尚未完成内容

明确指出当前没有实现：

* 领域模型；
* Agent；
* Reminder Dispatcher；
* Briefing Workflow；
* 业务页面。

## 7. 风险和注意事项

说明仍需关注的：

* 版本兼容；
* 环境变量；
* 开发与生产差异；
* 尚未验证的部署环节。

## 8. 下一步任务

只推荐一个下一步任务：

```text
实现 UserPreference 模型与统一时间工具。
```

不要自动继续实现下一阶段。
