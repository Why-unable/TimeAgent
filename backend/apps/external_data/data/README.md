# China administrative-area catalog

`china_administrative_areas.json` is the versioned province/city/county catalog
from `@vant/area-data` 2.1.0 (MIT). It is served only by the authenticated
backend catalog API and cached in process. Refresh it deliberately when an
administrative-division update is required; do not replace it with user input
or an unversioned browser-side list.
